{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    /* jest's globals, because this package runs jest rather than vitest —
       jest.config.js says why. */
    "types": ["jest"],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["**/*.ts", "**/*.tsx"],
  "exclude": ["node_modules", "android", ".expo"]
}
