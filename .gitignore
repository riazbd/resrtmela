# dependencies
node_modules/
.pnpm-store/

# builds
dist/
.next/
out/
.expo/
web-build/
# TypeScript's incremental build cache: a machine-local file that changes on
# every typecheck, so tracking it puts noise in a diff nobody can review
*.tsbuildinfo

# env
.env
.env.local
.env.*.local

# misc
*.log
.DS_Store
Thumbs.db
.turbo/
coverage/
